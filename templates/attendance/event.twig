
{% extends "dashboard/layout.twig" %}

{% block content %}
<div class="card-body">
    <table id="example1" class="table table-bordered table-striped">
        <thead>
            <tr>
              <th>Event</th>
              <th>Start Date</th>
              <th>End Date</th>
             <th></th>
            </tr>
            </thead>
            <tbody>
              {% for event in events %}
            <tr>
              <td><a href="/attendance/qr/{{event.org_id}}/{{event.event_id}}">{{event.event_name}}</a></td>
              <td>{{event.start_date}}</td>
              <td>{{event.end_date}}</td>
              <td><a href="/attendance/qr/{{event.org_id}}/{{event.event_id}}" class="btn btn-block bg-gradient-success btn-lg">Generate QR Codes</a></td>
            </tr>
            {%endfor%}
                  </tbody>
      <tfoot>
      {# <tr>
        <th>Rendering engine</th>
        <th>Browser</th>
        <th>Platform(s)</th>
        <th>Engine version</th>
        <th>CSS grade</th>
      </tr> #}
      </tfoot>
    </table>
  </div>
  {%endblock%}